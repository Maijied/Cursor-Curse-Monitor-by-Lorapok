# Deprecated — Admin API Worker

This standalone Cloudflare Worker (`website/admin-api/`) is **deprecated**.

Use **`website/admin/functions/api/`** (Cloudflare Pages Functions) instead — same origin as the admin SPA, full Firebase JWT verification, and parity with the React dashboard.

Do **not** deploy this Worker to production.
