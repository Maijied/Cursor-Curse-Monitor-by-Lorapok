name = "ccm-mail-relay"
main = "src/index.js"
compatibility_date = "2024-08-14"
account_id = "f049faaf2f67549f5c58837479596a4a"
workers_dev = false

# Workers binding — no REST API token required for outbound mail.
# Invoked only via Pages service binding; no public workers.dev URL.
[[send_email]]
name = "EMAIL"
