name = "ccm-stats-cron"
main = "src/index.js"
compatibility_date = "2024-08-14"
account_id = "f049faaf2f67549f5c58837479596a4a"
workers_dev = false

[triggers]
crons = ["* * * * *"]

[vars]
ADMIN_PUBLIC_URL = "https://cursor-dev.lorapok.tech"

# Sync via cred vault: npm run stats:cron:sync-vault --prefix website/admin
# CI admin-deploy runs deploy-stats-cron-ci.mjs when GitHub CRON_SECRET is set.
