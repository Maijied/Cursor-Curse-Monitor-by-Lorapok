# PR #233 merged — fix(admin): repair mail delivery and stale site-data across Mission Control

**Merged:** 2026-09-06T21:42:30Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/233  
**Branch:** `cursor/mail-site-data-fix-1244` → `main`  
**Stats:** +528 / -43 (9 files)

---

## Commits

- `fe6065d` fix(admin): repair mail delivery and stale site-data across Mission C…

---

## CI checks

- finalize: 
- Resolve Version (Live Marketplaces): SUCCESS
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Validate Dispatch Inputs: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy Admin Panel: SKIPPED
- Deploy to Marketplaces: SKIPPED
- Deploy Marketing Website: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
Fixes outbound mail failures and stale KPIs across Mission Control when cred vault/GitHub secrets are set but runtime delivery or dashboard data still breaks.

## Mail

- **Always sync** `CLOUDFLARE_EMAIL_API_TOKEN` to Pages — removed CI skip when relay exists (`enable-mail.mjs`)
- **Resend From alignment** — `resolveResendFromAddress()` rewrites `cursor.monitor@lorapok.tech` → `cursor.monitor@mail.lorapok.tech` when no `RESEND_FROM` override (`mail.js`)
- **Default `RESEND_FROM`** in `setup-resend-secret.mjs` for verified Resend subdomain

## Site-data

- **`fetchSiteDataWithLiveCache`** — when KV cache is missing or >60m old, fetches live marketplace channels read-only (no KV write) and overlays version/download fields
- **`useSiteData`** — cache-busts `/api/site-data` on manual refresh only

## CI (deploy-infra)

- Run `verify-resend-domain.mjs --verify --sync-kv` after Resend secret sync
- POST `/api/cron/stats-refresh` after successful Pages deploy to warm KV cache

## Tests

```bash
node website/admin/scripts/mail-resend-routing.test.mjs
node website/admin/functions/api/_shared/stats-refresh.test.mjs
node website/admin/scripts/verify-mail-transport.test.mjs
```

## Post-merge ops (cred vault machine)

```bash
node website/admin/scripts/verify-resend-domain.mjs --verify --sync-kv
node website/admin/scripts/repair-mail.mjs
npm run site:data && npm run site:seo
# workflow_dispatch → deploy-infra (Mission Control + website)
```

Plan: `plan/a3f8c2d1_mail-site-data-fix-plan.md`
<!-- CURSOR_AGENT_PR_BODY_END -->

<div><a href="https://cursor.com/agents/bc-9eea97f3-747c-40b0-a991-8211ecf11244?cursor_ref=pr_footer&cursor_cta=open_in_web"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-web-light.png"><img alt="Open in Web" width="114" height="28" src="https://cursor.com/as
