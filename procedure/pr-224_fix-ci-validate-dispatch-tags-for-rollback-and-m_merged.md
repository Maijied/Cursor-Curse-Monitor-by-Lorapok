# PR #224 merged — fix(ci): validate dispatch tags for rollback and metadata publish

**Merged:** 2026-09-05T11:56:25Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/224  
**Branch:** `fix/ci-dispatch-tag-validation` → `main`  
**Stats:** +557 / -3 (9 files)

---

## Commits

- `169e9a1` fix(ci): validate dispatch tags for rollback and metadata publish

---

## CI checks

- finalize: 
- Scan & triage npm advisories: SUCCESS
- Resolve Version (Live Marketplaces): SUCCESS
- Remediate patchable advisories: SKIPPED
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Validate Dispatch Inputs: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy Admin Panel: SKIPPED
- Deploy to Marketplaces: SKIPPED
- Deploy Marketing Website: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

## Summary
- Adds `release-tag-eligibility` classifier: which tags are **rollback-eligible**, **publish-ready**, or **metadata-ready** (sync-open-vsx / marketplace metadata).
- New CI job **`validate-dispatch`** runs before `release-prep` and `deploy` on workflow_dispatch for publish-tag, rollback, sync-open-vsx, and full-release — fails fast with suggested valid tags.
- Mission Control `/api/tags` returns `rollbackSources`, `publishReadyTags`, `metadataReadyTags`, and per-tag `eligibility`.
- Deploy API blocks invalid publish/rollback dispatches server-side.

## Test plan
- [x] `node website/admin/functions/api/_shared/release-tag-eligibility.test.mjs`
- [x] Admin vitest (publishable-tags, deployments-validation)
- [ ] CI `validate-dispatch` + full pipeline on PR
