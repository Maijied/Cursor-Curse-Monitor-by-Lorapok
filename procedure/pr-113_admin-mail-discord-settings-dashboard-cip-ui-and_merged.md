# PR #113 merged — Admin mail/Discord settings, dashboard CIP UI, and production mail probe

**Merged:** 2026-09-02T19:22:33Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/113  
**Branch:** `cursor/admin-mail-discord-settings-329a` → `main`  
**Stats:** +1571 / -218 (31 files)

---

## Commits

- `1f83073` docs(admin): close PR scope gaps from conversation audit
- `c87a4f4` fix(index): state-first reindex and stricter CIP import checks
- `baf6d27` feat(admin): mail transport settings and Lorapok Labs Family Discord …
- `58a2d9c` feat(dashboard): CIP export/import buttons and storage path display
- `639efa8` feat(admin): production mail probe script and health invite URL
- `daee0f7` fix: address CodeRabbit review on PR #113

---

## CI checks

- finalize: 
- Scan & triage npm advisories: SUCCESS
- Resolve Version (Live Marketplaces): SUCCESS
- Remediate patchable advisories: SKIPPED
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy to Marketplaces: SKIPPED
- Deploy Admin Panel: SKIPPED
- Deploy Marketing Website: SKIPPED

---

## Reviews

- coderabbitai: DISMISSED

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
Closes the conversation audit gaps from PR #111/#112 in a single follow-up PR.

## Admin — mail transport
- **MailTransportCard** on Settings: product/support From emails, ops BCC, Resend-first toggle, support display name
- **GET/PUT `/api/integrations/mail/config`** with KV-backed identities and strict write-path KV reads
- Read-only secret status (Resend, MAIL_RELAY, REST, Testmail) + Sync up link to Mailbox

## Admin — Lorapok Labs Family hook
- **DiscordCommunityCard** rebranded with editable `communityInviteUrl` in KV
- Discord hooks consolidated on Settings (community + deployment + feedback)
- Subscribe fallback invite fixed (`bp42QAMC6`); exposed via `buildPublicSiteConfig()` / health

## IDE dashboard — CIP + storage paths
- Export/Import `.cip.json` buttons in Data recovery section
- Active account label + `stateDbPath` / `searchDbPath` display
- Respects `cipExportEnabled` / `cipImportEnabled` policy flags

## Index P2 fixes
- Reindex commits state/sidebar DB first, then search DB; partial-failure message if search fails after state
- CIP import: reject missing `ownerHash` when cross-account disabled; verify `contentHash` matches title/turns
- CIP import rolls back open DB transactions on tamper detection (throw vs early return)

## Production mail validation tooling
- `website/admin/scripts/probe-mail-production.mjs`
- npm scripts: `mail:probe-production`, `mail:probe-production:e2e`

## CodeRabbit review fixes (latest commit)
- Fixed reindex `skipped` double-counting and reindex button stuck after CIP ops
- Mail KV write path no longer overwrites on transient read failure
- Subscribe fallback inherits community invite when subscribe URL not explicitly configured
- Welcome modal Discord link reads `discordInviteUrl` from site-config
- Mail/discord API validation tightened; dev API parity for mail env + body checks

## Post-merge manual validation
1. Deploy, then `npm run mail:probe-production --prefix website/admin`
2
