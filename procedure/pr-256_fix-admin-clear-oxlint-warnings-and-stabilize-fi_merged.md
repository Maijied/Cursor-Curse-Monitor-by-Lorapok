# PR #256 merged — fix(admin): clear oxlint warnings and stabilize Firestore rules deploy

**Merged:** 2026-09-09T05:07:02Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/256  
**Branch:** `cursor/ci-lint-firebase-fix-773e` → `main`  
**Stats:** +12021 / -3544 (55 files)

---

## Commits

- `a5e83cd` fix(admin): clear oxlint warnings and stabilize Firestore rules deploy

---

## CI checks

- finalize: 
- Scan & triage npm advisories: SUCCESS
- Resolve Version (Live Marketplaces): SUCCESS
- Remediate patchable advisories: SKIPPED
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Validate Dispatch Inputs: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy Admin Panel: SKIPPED
- Deploy to Marketplaces: SKIPPED
- Deploy Marketing Website: SKIPPED
- Notify Discord — marketing website: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
## Summary

Fixes the 14 oxlint warnings reported in CI and improves the Firestore rules deploy step that was failing with misleading npm deprecation noise.

## Oxlint (14 → 0 warnings)

- Removed unused imports/params in API handlers, shared modules, and tests
- Split non-component exports into dedicated modules to satisfy `react/only-export-components`:
  - `use-auth-session.ts` — auth hook + context
  - `deploy-runtime-context.ts` — deploy hooks + context
  - `settings-tab-storage.ts` — settings tab persistence
  - `marketplace-distribution-chart-utils.ts` — donut chart geometry helper

## Firebase deploy CI step

- Pin `firebase-tools@14.3.1` as a devDependency (avoids `npx firebase-tools@latest` drift on Node 24)
- Use local CLI binary with `--non-interactive`
- Filter npm deprecation stderr so failures surface the real Firebase error (e.g. auth) instead of `uuid@latest` noise

## Testing

- `npx oxlint` — 0 warnings
- `npm test` — 253/253 passed
- `npm run build` — passed
- Local `--rules-only` run surfaces clear auth error when credentials are invalid (expected in dev without SA JSON)

## Notes

- No merge conflict on `cursor/social-webhooks-773e` — branch was already merged with `main`. After this lands, rebase SOCIAL-01 on `main` to pick up these lint/import path changes (Settings tab storage split may need the `social` tab id added to `settings-tab-storage.ts`).
<!-- CURSOR_AGENT_PR_BODY_END -->

<div><a href="https://cursor.com/agents/bc-a68b3a8c-5f25-4f0d-b5ff-db42c2d5773e?cursor_ref=pr_footer&cursor_cta=open_in_web"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-web-light.png"><img alt="Open in Web" width="114" height="28" src="https://cursor.com/assets/images/open-in-web-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/background-agent?bcId=bc-a6
