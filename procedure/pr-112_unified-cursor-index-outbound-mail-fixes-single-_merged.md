# PR #112 merged — Unified Cursor Index + outbound mail fixes (single PR)

**Merged:** 2026-09-01T12:16:50Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/112  
**Branch:** `cursor/fix-reindex-configurable-329a` → `main`  
**Stats:** +3689 / -69 (38 files)

---

## Commits

- `2190d35` feat(admin): fix outbound mail with Resend-first routing and Mailbox …
- `3520c5f` fix(reindex): configurable live vs quit-first policy from Mission Con…
- `36ea026` feat(dashboard): show reindex progress loader with phase updates
- `c3377d1` feat: unified cursor index system with admin limits and dual-DB merge
- `b03ad8a` Merge remote-tracking branch 'origin/cursor/fix-mail-outbound-testmai…
- `212c364` test: add cursor index validation, corner cases, and merge mail PR
- `62e8bb9` fix: address CodeRabbit review on cursor index and mail
- `e46f22e` fix: close remaining CodeRabbit review items

---

## CI checks

- finalize: 
- Scan & triage npm advisories: SUCCESS
- Resolve Version (Live Marketplaces): SUCCESS
- Remediate patchable advisories: SKIPPED
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy to Marketplaces: SKIPPED
- Deploy Admin Panel: SKIPPED
- Deploy Marketing Website: SKIPPED
- CodeRabbit: SUCCESS

---

## Reviews

- coderabbitai: DISMISSED

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
## Summary

Addresses CodeRabbit review findings on PR #112 (unified Cursor Index + outbound mail).

### Cursor index / CIP
- Enforce `cipAllowCrossUserLocalImport` and `cipDedupeAcrossUsers` on import
- Block export when sanitization is required but secrets remain after redaction
- Commit state DB before search DB; close search DB if state DB open fails
- Tie CIP export/import to master `indexEnabled`
- Read local `reindexWritePolicy` VS Code setting (matches package.json)
- Admin PATCH merges omit `undefined` so partial updates do not reset omitted fields
- Reindex/CIP backups use `createFullBackup()`; dashboard reindex wrapped in try/catch

### Mail / testmail
- Do not fall back to `MAIL_RELAY` when Resend is the primary transport for external/testmail inboxes
- Validate testmail API `result === "success"`; Mailbox poll stops on terminal states (no interval respawn)
- Use `crypto.randomUUID()` for testmail probe tags; resolve account ID from mail env in setup script
- Legacy reindex config API returns 400 for `null` JSON body

### Tests
- Mock `fetch` in cursor index policy test (no live Mission Control dependency)
- Tighten live reindex policy assertion to verify editor gate is bypassed
- Extended cursor-index config, CIP validation, and admin config script tests

## Test plan
- [x] `npm run compile`
- [x] `node tests/test_cursor_index_config.js`
- [x] `node tests/test_cip_validation.js`
- [x] `node tests/test_reindex_policy.js`
- [x] `node website/admin/scripts/cursor-index-config.test.mjs`
- [x] `npm test -- --run src/__tests__/reindex-config-api.test.ts` (admin)
<!-- CURSOR_AGENT_PR_BODY_END -->

<div><a href="https://cursor.com/agents/bc-14a926ec-6511-4ac5-8d3f-4fb45466329a?cursor_ref=pr_footer&cursor_cta=open_in_web"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open
