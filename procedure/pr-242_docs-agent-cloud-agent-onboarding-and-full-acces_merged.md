# PR #242 merged — docs(agent): CLOUD-AGENT onboarding and full-access checklist

**Merged:** 2026-09-08T02:45:48Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/242  
**Branch:** `cursor/cloud-agent-ready-a873` → `main`  
**Stats:** +288 / -14 (8 files)

---

## Commits

- `274b1c0` docs(agent): add CLOUD-AGENT onboarding and full-access checklist

---

## CI checks

- finalize: 
- Resolve Version (Live Marketplaces): 
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Validate Dispatch Inputs: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

## Summary

- Adds durable [`.cursor/CLOUD-AGENT.md`](.cursor/CLOUD-AGENT.md): cloud vs local capability matrix, GitHub `403` root cause (narrow App run token), `GH_TOKEN` PAT workaround, cred vault via `CRED_STORE_GPG_BASE64` + `CRED_VAULT_PASSPHRASE`, browser differences, and maintainer checklist.
- Updates `AGENT_INIT.md` read order to pick LOCAL vs CLOUD by environment.
- Expands `TASK-TRACKING.md` cloud section with actionable full-access steps.
- Touches `environment.json` install (Node version guard + `gh --version` sanity check), `cred-vault.mdc`, `AGENTS.md`, `LOCAL-AGENT.md` cross-links.

**Note:** Includes the LOCAL-AGENT commit from #241 so onboarding is complete in one PR. If #241 merges first, this branch can be rebased to drop the duplicate commit.

## Maintainer actions for cloud parity (not in repo)

1. [Cloud Agents → Secrets](https://cursor.com/dashboard/cloud-agents): add **`GH_TOKEN`** (fine-grained PAT with Issues + Pull requests read/write; classic `repo` + `project` for Project #4 sync).
2. Optional: **`CRED_VAULT_PASSPHRASE`** + **`CRED_STORE_GPG_BASE64`** (same as `admin-production` CI).
3. Rebuild Cloud environment after merge.

## Test plan

- [x] Docs only — no code behavior change
- [x] `secretlint` + governance hook passed on commit
- [ ] Maintainer verifies `gh issue list` in a cloud run after adding `GH_TOKEN`
