# PR #116 merged — fix: account switch sync, external mail via Resend, and usage-discord footer arch

**Merged:** 2026-09-03T15:28:59Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/116  
**Branch:** `fix/unified-account-mail-issues` → `main`  
**Stats:** +151 / -96 (14 files)

---

## Commits

- `93850df` fix: preserve account switch on passive token capture and refresh fix…

---

## CI checks

- finalize: 
- Resolve Version (Live Marketplaces): SUCCESS
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy Admin Panel: SKIPPED
- Deploy to Marketplaces: SKIPPED
- Deploy Marketing Website: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

## Summary

- **Account switch sync:** Browser `saveToken` preserves the manually selected `activeAccountId` on passive cookie/page capture; explicit Options paste still activates the new account. Usage warnings reset when switching accounts (IDE + browser).
- **Mail:** Resend-first routing for external recipients is already on `main`; this PR hardens `verify-mail-transport` test isolation. **Post-merge ops:** run `node website/admin/scripts/setup-resend-secret.mjs` (or `repair-mail.mjs`) so production `mailResendConfigured` is true.
- **Dynamic fix notice:** `releaseHighlights` flow into `site-data.json`, `generate-site-data.mjs`, and embedded `generated-dev-notice` with bullets for account sync, Resend mail, and usage/discord polish.
- **Procedures:** Updates for issues #105 and #109 tracking.

Closes #105
Closes #109

## Test plan

- [x] `npm test` (root) — verify-mail-transport test fixed for cred-vault isolation
- [x] `npm run browser-ext:test`
- [x] `cd website/admin && npm test` (159 tests)
- [x] `npm run compile`
- [ ] Post-merge: `GET /api/health` → `mailResendConfigured: true` after Resend secret sync
- [ ] Manual: browser with 2 accounts — switch to B, visit cursor.com as A — active stays B
