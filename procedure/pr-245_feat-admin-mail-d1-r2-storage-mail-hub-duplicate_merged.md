# PR #245 merged — feat(admin): Mail D1/R2 storage, Mail hub, duplicate subscribe guard

**Merged:** 2026-09-08T10:22:44Z  
**URL:** https://github.com/Maijied/Cursor-Curse-Monitor-by-Lorapok/pull/245  
**Branch:** `feat/mail-d1-r2-aliases-validation` → `main`  
**Stats:** +4637 / -251 (79 files)

---

## Commits

- `a238599` feat(admin): mail D1/R2 storage, Mail hub, duplicate subscribe guard
- `28d1578` feat(admin): configurable mail identities with per-identity test send
- `55ae39e` fix(admin): provision and audit inbound Email Routing for @lorapok.tech
- `0b81481` fix(admin): configurable mail forward default and CI TypeScript errors
- `c045e99` fix(ci): stabilize Discord deploy notifications and version tag
- `d5b908a` fix(ci): fall back to ADMIN_KV for Discord deploy webhook
- `fb6bf45` fix(ci): let Discord notify script resolve webhook from ADMIN_KV
- `5116a77` docs(discord): clarify version source for deploy notify cards

---

## CI checks

- finalize: 
- Scan & triage npm advisories: SUCCESS
- Resolve Version (Live Marketplaces): SUCCESS
- Remediate patchable advisories: SKIPPED
- Admin Deploy Gate: SKIPPED
- SEO & Metadata Pipeline: SKIPPED
- Validate Dispatch Inputs: SKIPPED
- Build & Validate (Root Extension): SUCCESS
- Browser Extension CI: SUCCESS
- Admin Panel CI: SUCCESS
- Prepare Release Tag (push to main): SKIPPED
- Prepare & Tag Release: SKIPPED
- Test Results Summary: SUCCESS
- Deploy Admin Panel: SKIPPED
- Deploy to Marketplaces: SKIPPED
- Deploy Marketing Website: SKIPPED

---

## Reviews

_No reviews recorded._

---

## Retrospective notes

_Auto-generated by `scripts/procedure-finalize-pr.mjs`. Append manual verification (Tier C) and deploy notes below._

### Post-merge verification

- [ ] Admin deploy (if `website/admin/functions` changed)
- [ ] Production smoke documented in linked procedure file
- [ ] GitHub issue closed

---

## PR body (excerpt)

## Summary

- **Mail infrastructure** — D1 `mail_messages` / `mail_audit_resend`, R2 audit blobs, `ccm-mail-audit` worker, KV scatter aliases API, mail replay service + `resend-saved-mail.mjs` (vault redirect default; fixes dry-run `env is not defined`)
- **Mission Control Mail tab** (`/dashboard/mail`) — transport checklist, Cloudflare mail config, email identities, Resend settings (incl. domain verified toggle), masked `MAIL_REDIRECT_TO`, link to Mailbox
- **Subscribe validation** — `POST /api/subscribe` returns **409** `{ ok: false, error: "already_subscribed" }` for duplicate emails; marketing site shows friendly “already subscribed” message
- **KV fixes** — skip flags for mailbox/usage/audit on replay; improved `kv-put` / service-usage paths

## Test plan

- [x] `npm test -- --run src/__tests__/subscribe-api.test.ts src/__tests__/kv-put.test.ts src/__tests__/mailbox-storage.test.ts`
- [x] `node website/admin/functions/api/_shared/mail-aliases.test.mjs` (+ mask-email, mail-storage, services/mail/config)
- [x] `node website/admin/scripts/resend-saved-mail.mjs --dry-run` (no `env is not defined`)
- [ ] Mission Control → **Mail** tab loads transport + identities + redirect card
- [ ] Subscribe same email twice → 409 on API; site form shows “already subscribed”
- [ ] Mailbox compose still works after deploy

## Post-merge deploy steps

1. **D1 schema** (not run in CI):
   ```bash
   cd website/admin
   npx wrangler d1 execute ccm-admin-d1 --remote --file=./d1/schema.sql
   ```
2. **Mail audit worker** (if not already deployed):
   ```bash
   cd website/admin/workers/mail-audit && npx wrangler deploy
   ```
3. **Full mail repair** (optional, for relay/token sync):
   ```bash
   node website/admin/scripts/repair-mail.mjs
   ```

## Docs

- `docs/wiki/Mailbox-and-Email.md` — D1/R2 storage, Mail tab, subscribe validation, replay script
- `CHANGELOG.md` — user-visible mail improvements
