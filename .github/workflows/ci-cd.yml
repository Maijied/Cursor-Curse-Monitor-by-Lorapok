name: CI/CD

# ─────────────────────────────────────────────────────────────────────────────
# Unified workflow for Cursor Curse Monitor
#
# Triggers:
#   PR to main        → ci (build/validate only)
#   Push to main      → ci (build dynamic VSIX artifact with commit hash) -> website
#   workflow_dispatch → release bump → deploy to marketplaces → website
#   Tag v*            → deploy to marketplaces → website
# ─────────────────────────────────────────────────────────────────────────────

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish_market:
        description: "Publish Market"
        required: true
        type: choice
        options:
          - Both
          - VS Code Marketplace
          - Open VSX
        default: Both
      release_channel:
        description: "Release Channel"
        required: true
        type: choice
        options:
          - Production
          - Beta (Pre-release)
        default: Production
      version_type:
        description: "Release Type (Tagging Rules)"
        required: true
        type: choice
        options:
          - "patch - Bug fix or Feature update (e.g. v0.7.1, v0.7.2)"
          - "minor - New Feature (e.g. v0.8.0)"
          - "major - Production / Breaking (e.g. v1.0.0)"
          - "custom - Specify exact version below"
        default: "patch - Bug fix or Feature update (e.g. v0.7.1, v0.7.2)"
      custom_version:
        description: "Custom version (e.g. 0.6.0-beta.1)"
        required: false
        type: string

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  # ───────────────────────────────────────────────────────────────────────────
  # 1. CI — Build & Validate (Runs on every trigger)
  # ───────────────────────────────────────────────────────────────────────────
  ci:
    name: Build & Validate
    runs-on: ubuntu-latest
    if: >-
      !(github.event_name == 'push'
        && !startsWith(github.ref, 'refs/tags/')
        && contains(github.event.head_commit.message, 'chore: release'))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Compile TypeScript
        run: npm run compile

      - name: Validate icon & logo assets
        run: npm run validate:assets

      - name: Secret scan (secretlint)
        run: npm run security:scan

      - name: Dynamically inject commit hash (Push to main only)
        if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
        run: |
          CURRENT=$(node -p "require('./package.json').version")
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          NEW_VERSION="${CURRENT}-beta.${SHORT_SHA}"
          npm version --no-git-tag-version "$NEW_VERSION"
          echo "Dynamic version: $NEW_VERSION"

      - name: Package VSIX
        run: npm run package

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: cursor-curse-monitor-vsix-${{ github.sha }}
          path: "*.vsix"
          retention-days: 14
          if-no-files-found: error

  browser-extension-ci:
    name: Browser Extension CI
    runs-on: ubuntu-latest
    needs: ci
    if: >-
      !(github.event_name == 'push'
        && !startsWith(github.ref, 'refs/tags/')
        && contains(github.event.head_commit.message, 'chore: release'))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build -w @lorapok/cursor-monitor-shared

      - name: Test browser extension
        run: npm run browser-ext:test

      - name: Build browser extension
        run: npm run browser-ext:build

      - name: Validate AMO metadata (dry-run)
        run: |
          node browser-extension/scripts/generate-amo-metadata.mjs
          node browser-extension/scripts/validate-amo-metadata.mjs

      - name: Build Chrome zip
        run: npm run build:chrome -w browser-extension

      - name: Upload browser extension artifacts
        uses: actions/upload-artifact@v4
        with:
          name: browser-extension-${{ github.sha }}
          path: |
            browser-extension/dist/
            browser-extension/artifacts/chrome/*.zip
          retention-days: 14
          if-no-files-found: error

  admin-ci:
    name: Admin Panel CI
    runs-on: ubuntu-latest
    needs: ci
    if: >-
      !(github.event_name == 'push'
        && !startsWith(github.ref, 'refs/tags/')
        && contains(github.event.head_commit.message, 'chore: release'))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            website/admin/package-lock.json

      - name: Install root dependencies (SEO scripts)
        run: npm ci

      - name: Generate site artifacts for admin build
        run: |
          node scripts/generate-site-data.mjs
          node scripts/generate-seo.mjs

      - name: Install admin dependencies
        working-directory: website/admin
        run: npm ci

      - name: Test admin panel
        working-directory: website/admin
        env:
          ADMIN_MASTER_EMAIL: ${{ secrets.ADMIN_MASTER_EMAIL }}
        run: npm test

      - name: Build admin panel
        working-directory: website/admin
        run: npm run build

      - name: Assert admin static artifacts in dist
        working-directory: website/admin
        run: test -f dist/site-data.json && test -f dist/seo.json

  admin-deploy:
    name: Deploy Admin Panel
    runs-on: ubuntu-latest
    needs: admin-ci
    if: >-
      github.event_name == 'push'
      && github.ref == 'refs/heads/main'
      && !contains(github.event.head_commit.message, 'chore: release')
    environment:
      name: admin-production
      url: https://cursor-dev.lorapok.tech
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            website/admin/package-lock.json

      - name: Install root dependencies (SEO scripts)
        run: npm ci

      - name: Generate site artifacts
        run: |
          node scripts/generate-site-data.mjs
          node scripts/generate-seo.mjs

      - name: Install admin dependencies
        working-directory: website/admin
        run: npm ci

      - name: Build admin panel
        working-directory: website/admin
        run: npm run build

      - name: Check Cloudflare secrets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "::error::CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID must be set in the admin-production environment."
            exit 1
          fi

      - name: Sync Pages runtime secret for outbound email
        working-directory: website/admin
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_EMAIL_API_TOKEN: ${{ secrets.CLOUDFLARE_EMAIL_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          MAIL_TOKEN="${CLOUDFLARE_EMAIL_API_TOKEN:-$CLOUDFLARE_API_TOKEN}"
          if [ -z "$MAIL_TOKEN" ]; then
            echo "::error::Set CLOUDFLARE_EMAIL_API_TOKEN (preferred) or CLOUDFLARE_API_TOKEN with Email Sending → Send permission."
            exit 1
          fi
          printf '%s' "$MAIL_TOKEN" | npx wrangler pages secret put CLOUDFLARE_EMAIL_API_TOKEN --project-name=cursor-monitor-admin
          npx wrangler email sending enable lorapok.tech || echo "::warning::Onboard lorapok.tech in Dashboard → Email Service if sends return 401."

      - name: Deploy to Cloudflare Pages
        working-directory: website/admin
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy dist --project-name=cursor-monitor-admin --branch=main

  # ───────────────────────────────────────────────────────────────────────────
  # 2. RELEASE TRIGGER — Manual version bump via workflow_dispatch
  # ───────────────────────────────────────────────────────────────────────────
  release-bump:
    name: Prepare Release
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    outputs:
      new_version: ${{ steps.bump.outputs.new_version }}
      tag: ${{ steps.bump.outputs.tag }}
      bumped: ${{ steps.bump.outputs.bumped }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Calculate and apply version bump
        id: bump
        run: |
          # Extract the base action type (e.g. "patch" from "patch - Bug fix...")
          BUMP_TYPE=$(echo "${{ inputs.version_type }}" | awk '{print $1}')
          CHANNEL="${{ inputs.release_channel }}"

          if [ "$BUMP_TYPE" == "custom" ]; then
            if [ -z "${{ inputs.custom_version }}" ]; then
              echo "Error: custom_version is required when version_type is custom."
              exit 1
            fi
            NEW_VERSION="${{ inputs.custom_version }}"
            echo "Using custom version: $NEW_VERSION"
            npm version --no-git-tag-version "$NEW_VERSION"
          elif [ "$CHANNEL" == "Beta (Pre-release)" ]; then
            node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json')); pkg.version=pkg.version.split('-')[0]; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));"
            CURRENT=$(node -p "require('./package.json').version")
            echo "Current base version: $CURRENT — beta $BUMP_TYPE bump"
            npm version $BUMP_TYPE --no-git-tag-version --preid=beta
            NEW_VERSION=$(node -p "require('./package.json').version")
            echo "Auto-bumped beta $BUMP_TYPE: $NEW_VERSION"
          else
            node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json')); pkg.version=pkg.version.split('-')[0]; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));"
            CURRENT=$(node -p "require('./package.json').version")
            echo "Current base version: $CURRENT"
            npm version $BUMP_TYPE --no-git-tag-version
            NEW_VERSION=$(node -p "require('./package.json').version")
            echo "Auto-bumped $BUMP_TYPE: $NEW_VERSION"
          fi

          TAG="v$NEW_VERSION"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "chore: release v$NEW_VERSION"
          git tag "$TAG"
          git push origin main
          git push origin "$TAG"

          echo "new_version=$NEW_VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "bumped=true" >> "$GITHUB_OUTPUT"

          echo "## Release Version Created" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** $NEW_VERSION (${{ inputs.release_channel }})" >> $GITHUB_STEP_SUMMARY

  # ───────────────────────────────────────────────────────────────────────────
  # 3. DEPLOY — Publish to Marketplaces
  # ───────────────────────────────────────────────────────────────────────────
  deploy:
    name: Deploy to Marketplaces
    runs-on: ubuntu-latest
    needs: [release-bump]
    if: always() && (needs.release-bump.outputs.bumped == 'true' || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')))
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    env:
      VSCE_PUBLISHER: "LorapokLabs"
      OVSX_PUBLISHER: "lorapok-labs"
    steps:
      - name: Checkout latest
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-bump.outputs.tag || github.ref }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies & Compile
        run: |
          npm ci
          npm run compile
          npm run package

      - name: Install publishing tools
        run: |
          npm install -g @vscode/vsce
          npm install -g ovsx

      - name: Resolve Configuration
        id: config
        run: |
          VERSION="${{ needs.release-bump.outputs.new_version }}"
          if [ -z "$VERSION" ]; then
            if [[ "${{ github.ref_type }}" == "tag" ]]; then
              VERSION="${GITHUB_REF#refs/tags/v}"
            else
              VERSION="$(node -p "require('./package.json').version")"
            fi
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          # Determine pre-release flag
          PRE_RELEASE=""
          if [[ "${{ inputs.release_channel }}" == "Beta (Pre-release)" ]]; then
            PRE_RELEASE="true"
          fi
          # Also fallback to auto-detecting beta from tag if triggered by push tag
          if [[ "$VERSION" == *"beta"* ]] || [[ "$VERSION" == *"rc"* ]] || [[ "$VERSION" == *"alpha"* ]]; then
             PRE_RELEASE="true"
          fi
          echo "pre_release=$PRE_RELEASE" >> "$GITHUB_OUTPUT"

          # Determine target markets
          DO_OVSX="false"
          DO_VSCE="false"
          TARGET="${{ inputs.publish_market }}"
          if [[ -z "$TARGET" || "$TARGET" == "Both" ]]; then
             DO_OVSX="true"; DO_VSCE="true"
          elif [[ "$TARGET" == "Open VSX" ]]; then
             DO_OVSX="true"
          elif [[ "$TARGET" == "VS Code Marketplace" ]]; then
             DO_VSCE="true"
          fi
          echo "do_ovsx=$DO_OVSX" >> "$GITHUB_OUTPUT"
          echo "do_vsce=$DO_VSCE" >> "$GITHUB_OUTPUT"

      # ── Open VSX (canonical lorapok-labs namespace) ───────────────────────
      - name: Publish to Open VSX
        if: steps.config.outputs.do_ovsx == 'true'
        run: |
          if [ -z "$OVSX_PAT" ]; then
            echo "::warning::OVSX_PAT not set — skipping Open VSX publish"
            exit 0
          fi
          PRE_FLAG=""
          if [ "${{ steps.config.outputs.pre_release }}" = "true" ]; then
            PRE_FLAG="--pre-release"
          fi
          node scripts/publish-ovsx.mjs $PRE_FLAG
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}

      - name: Verify marketplace sync
        if: steps.config.outputs.do_ovsx == 'true'
        run: node scripts/verify-marketplace-sync.mjs --strict --retry=12

      # ── VS Code Marketplace ──────────────────────────────────────────────
      - name: Publish to VS Code Marketplace
        if: steps.config.outputs.do_vsce == 'true'
        run: |
          if [ -z "$VSCE_PAT" ]; then
            echo "::warning::VSCE_PAT not set — skipping VS Code Marketplace publish"
            exit 0
          fi
          echo "Publishing to VS Code Marketplace (pre_release=${{ steps.config.outputs.pre_release }})..."
          set +e
          OUTPUT=$(vsce publish -p "$VSCE_PAT" $( [ "${{ steps.config.outputs.pre_release }}" = "true" ] && echo --pre-release ) 2>&1)
          CODE=$?
          echo "$OUTPUT"
          if [ $CODE -ne 0 ]; then
            if echo "$OUTPUT" | grep -qiE "already exists|already been published"; then
              echo "::warning::Version already on VS Code Marketplace — treating as success"
              exit 0
            fi
            exit $CODE
          fi
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      # ── Browser extension (Firefox AMO + Chrome zip) ─────────────────────
      - name: Build browser extension
        run: |
          npm run build -w @lorapok/cursor-monitor-shared
          npm run browser-ext:build
          npm run build:chrome -w browser-extension

      - name: Generate & validate AMO metadata
        run: |
          node browser-extension/scripts/generate-amo-metadata.mjs --version=${{ steps.config.outputs.version }}
          node browser-extension/scripts/validate-amo-metadata.mjs

      - name: Sign & submit to Firefox Add-ons (AMO)
        run: |
          if [ -z "$AMO_JWT_ISSUER" ] || [ -z "$AMO_JWT_SECRET" ]; then
            echo "::warning::AMO_JWT_* not set — skipping Firefox AMO publish"
            exit 0
          fi
          mkdir -p browser-extension/artifacts/firefox
          npx web-ext@8 sign \
            --source-dir browser-extension/dist \
            --artifacts-dir browser-extension/artifacts/firefox \
            --channel listed \
            --amo-metadata browser-extension/amo/amo-metadata.generated.json \
            --api-key "$AMO_JWT_ISSUER" \
            --api-secret "$AMO_JWT_SECRET"
        env:
          AMO_JWT_ISSUER: ${{ secrets.AMO_JWT_ISSUER }}
          AMO_JWT_SECRET: ${{ secrets.AMO_JWT_SECRET }}

      - name: Verify AMO listing status
        run: node browser-extension/scripts/verify-amo-status.mjs --slug=cursor-curse-monitor-by-lorapok
        env:
          AMO_JWT_ISSUER: ${{ secrets.AMO_JWT_ISSUER }}
          AMO_JWT_SECRET: ${{ secrets.AMO_JWT_SECRET }}

      - name: Stage Chrome zip for website
        run: |
          mkdir -p website/downloads
          cp browser-extension/artifacts/chrome/*.zip "website/downloads/cursor-curse-monitor-chrome-${{ steps.config.outputs.version }}.zip"

      # ── GitHub Release ────────────────────────────────────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.release-bump.outputs.tag || github.ref_name }}
          name: Cursor Curse Monitor v${{ steps.config.outputs.version }}
          files: |
            *.vsix
            browser-extension/artifacts/firefox/*.xpi
            browser-extension/artifacts/chrome/*.zip
          generate_release_notes: true
          prerelease: ${{ steps.config.outputs.pre_release == 'true' }}
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy summary
        run: |
          echo "## Deploy Summary" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ steps.config.outputs.version }} (pre_release=${{ steps.config.outputs.pre_release }})" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.config.outputs.do_ovsx }}" == "true" ]; then
             echo "**Open VSX:** Published" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "${{ steps.config.outputs.do_vsce }}" == "true" ]; then
             echo "**VS Code Marketplace:** Published" >> $GITHUB_STEP_SUMMARY
          fi
          echo "**Firefox AMO:** Signed XPI attached (if credentials configured)" >> $GITHUB_STEP_SUMMARY
          echo "**Chrome:** Direct-download zip attached" >> $GITHUB_STEP_SUMMARY

  # ───────────────────────────────────────────────────────────────────────────
  # 4. WEBSITE — Deploy GitHub Pages
  # ───────────────────────────────────────────────────────────────────────────
  website:
    name: Deploy Website
    # Only refresh public site-data after marketplace deploy succeeds.
    needs: [deploy]
    if: always() && github.ref == 'refs/heads/main' && (needs.deploy.result == 'success' || needs.deploy.result == 'skipped')
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    concurrency:
      group: pages
      cancel-in-progress: false
    steps:
      - name: Checkout latest
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Generate dynamic site data
        run: node scripts/generate-site-data.mjs

      - name: Generate SEO artifacts
        run: |
          node scripts/generate-seo.mjs
          node scripts/validate-seo.mjs

      - name: Prepare site
        run: |
          mkdir -p _site
          cp -r website/. _site/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
